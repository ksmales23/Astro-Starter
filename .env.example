# Copy to .env for local dev, and set the same vars in Cloudflare Pages
# (Settings -> Environment variables). PUBLIC_ prefix = exposed to the client,
# inlined at build time, so changing it requires a redeploy.
PUBLIC_GTM_ID=GTM-XXXXXXX
